/* ==========================================================================
 Service.html
========================================================================== */

/* ========================================================= SERVICES HERO & INTRO ARCHITECTURE ========================================================= */
/* ========================================================= INDEX HERO PORTION - ARCHITECTURE & CAROUSEL LAYOUT ========================================================= */
.service-page-hero.page-hero-image { position: relative; padding: 22px 0 34px; background: radial-gradient(circle at 12% 6%, rgba(217, 185, 109, 0.12), transparent 28%), radial-gradient(circle at 88% 12%, rgba(21, 87, 127, 0.10), transparent 34%), linear-gradient(180deg, #ffffff 0%, #f7fbff 100%); overflow: hidden; }
body.dark-mode .service-page-hero.page-hero-image { background: radial-gradient(circle at 12% 6%, rgba(217, 185, 109, 0.12), transparent 28%), radial-gradient(circle at 88% 12%, rgba(47, 111, 159, 0.14), transparent 34%), linear-gradient(180deg, #071827 0%, #0d2338 100%); }
.service-page-hero .hero-landscape-card { position: relative; width: min(100% - 48px, 1720px); margin-inline: auto; min-height: clamp(440px, 58vh, 620px); height: clamp(440px, 58vh, 620px); display: block; border-radius: 20px; overflow: hidden; border: 1px solid rgba(21, 87, 127, 0.14); box-shadow: 0 22px 62px rgba(6, 36, 58, 0.14); background: var(--blue-deep); }
body.dark-mode .service-page-hero .hero-landscape-card { border-color: rgba(217, 185, 109, 0.2); box-shadow: 0 22px 62px rgba(0, 0, 0, 0.36); }
.service-page-hero .hero-landscape-card::before { content: ""; position: absolute; inset: 0; background: linear-gradient(90deg, rgba(7,24,39,.72) 0%, rgba(7,24,39,.52) 34%, rgba(7,24,39,.28) 62%, rgba(7,24,39,.05) 100%); z-index: 1; pointer-events: none; }
.service-page-hero .hero-landscape-card::after { content: ""; position: absolute; inset: auto 0 0; height: 40%; background: linear-gradient(180deg, transparent 0%, rgba(7, 24, 39, 0.52) 100%); z-index: 2; pointer-events: none; }
.service-page-hero .hero-landscape-card img { width: 100%; height: 100%; min-height: 100%; object-fit: cover; object-position: center right; transition: transform 0.9s ease; }
.service-page-hero .hero-landscape-card:hover img { transform: scale(1.025); }
.service-page-hero .hero-overlay-content { position: absolute; left: clamp(42px, 6vw, 96px); top: 50%; transform: translateY(-50%); z-index: 4; max-width: 610px; padding: 0; display: block; }
.service-page-hero .hero-overlay-content .section-kicker { color: var(--gold-bright); margin-bottom: 12px; font-weight: 700; letter-spacing: 0.04em; text-transform: none; font-size: 0.95rem; display: inline-block; }
.service-page-hero .hero-overlay-content h2 { font-size: clamp(2.2rem, 4.15vw, 4.25rem); line-height: 1.08; font-weight: 700; letter-spacing: -0.052em; color: var(--white); margin-bottom: 16px; text-align: left; }
.service-page-hero .hero-overlay-content p { max-width: 560px; color: rgba(255, 255, 255, 0.84); font-size: 0.95rem; line-height: 1.65; margin-bottom: 22px; text-align: left; }
.service-page-hero .hero-buttons { display: flex; align-items: center; gap: 14px; flex-wrap: wrap; }
.service-page-hero .hero-buttons .btn { min-height: 42px; padding: 0 20px; font-size: 0.86rem; width: auto; justify-content: center; }
.service-page-hero .hero-buttons .btn-primary { background: linear-gradient(135deg, var(--blue-main), var(--blue-dark)); color: var(--white); box-shadow: 0 14px 30px rgba(21, 87, 127, 0.28); }
.service-page-hero .hero-buttons .btn-primary:hover { background: linear-gradient(135deg, var(--blue-dark), var(--blue-mid)); }
.service-page-hero .hero-buttons .btn-light { background: rgba(255, 255, 255, 0.14); color: var(--white); border-color: rgba(255, 255, 255, 0.25); backdrop-filter: blur(12px); }
.service-page-hero .hero-buttons .btn-light:hover { background: var(--white); color: var(--blue-deep); }

/* ========================================================= INDEX STYLE INTRO DARK BOX ELEMENTS ========================================================= */
.intro-dark-section { position: relative; overflow: hidden; padding: 78px 0; color: var(--white); background: radial-gradient(circle at 88% 0%, rgba(217, 185, 109, 0.2), transparent 34%), linear-gradient(135deg, var(--blue-deep), var(--blue-dark) 58%, #123b5e); text-align: left; border-bottom: none; }
.intro-dark-section::before { content: ""; position: absolute; width: 440px; height: 440px; border-radius: 50%; right: -160px; top: -190px; background: rgba(217, 185, 109, 0.12); pointer-events: none; }
.intro-dark-section::after { content: ""; position: absolute; left: -100px; bottom: -160px; width: 300px; height: 300px; border: 1px solid rgba(217, 185, 109, 0.2); border-radius: 50%; pointer-events: none; }
.intro-dark-box { max-width: 1200px; margin: 0 auto; position: relative; z-index: 2; align-items: flex-start; display: block; }
.intro-dark-box h2 { font-size: clamp(1.9rem, 4vw, 3.4rem); line-height: 1.08; font-weight: 950; letter-spacing: -0.06em; margin: 12px 0 18px; color: var(--white); text-align: left; }
.intro-dark-box p { max-width: 760px; color: rgba(255, 255, 255, 0.78); font-size: 1rem; margin-bottom: 15px; text-align: left; }
.intro-dark-box .btn { margin-top: 10px; width: auto; display: inline-flex; }

/* ========================================================= RESPONSIVE BREAKDOWN RULES ========================================================= */
@media (max-width: 992px) { .service-page-hero .hero-landscape-card { width: min(100% - 32px, 1720px); min-height: clamp(400px, 50vh, 550px); height: clamp(400px, 50vh, 550px); } .service-page-hero .hero-overlay-content { left: clamp(30px, 5vw, 50px); max-width: 85%; } }
@media (max-width: 768px) { .service-page-hero .hero-landscape-card { min-height: 460px; height: 460px; } .service-page-hero .hero-overlay-content { position: relative; left: 0; top: 0; transform: none; padding: 40px 24px; max-width: 100%; } .service-page-hero .hero-landscape-card { display: flex; flex-direction: column; height: auto; min-height: auto; } .service-page-hero .hero-landscape-card img { height: 260px; width: 100%; } .service-page-hero .hero-landscape-card::before { background: linear-gradient(180deg, rgba(7,24,39,0.3) 0%, rgba(7,24,39,0.85) 100%); } .intro-dark-section { padding: 55px 0; } }
@media (max-width: 480px) { .service-page-hero .hero-buttons { flex-direction: column; width: 100%; } .service-page-hero .hero-buttons .btn { width: 100%; } }

/* ========================================================= CORE SERVICES & MAIN DETAIL GRID ========================================================= */
.services-page-section { padding: clamp(60px, 8vh, 100px) 0; background: var(--bg-main); }
.section-heading.center { text-align: center; max-width: 720px; margin: 0 auto 55px; }
.section-heading.center h2 { font-size: clamp(1.8rem, 3vw, 2.4rem); font-weight: 800; color: var(--blue-dark); margin-bottom: 14px; letter-spacing: -0.02em; }
body.dark-mode .section-heading.center h2 { color: var(--white); }
.section-heading.center p { color: var(--text-muted); font-size: 1rem; line-height: 1.6; }
.services-detail-grid { display: grid; grid-template-columns: repeat(2, 1fr); gap: 40px; }
.service-detail-card { background: var(--bg-card); border: 1px solid var(--border-light); border-radius: var(--radius-xl); overflow: hidden; box-shadow: var(--shadow-sm); display: flex; flex-direction: column; transition: transform 0.3s ease, border-color 0.3s ease, box-shadow 0.3s ease; }
.service-detail-card:hover { transform: translateY(-6px); border-color: var(--border-gold); box-shadow: var(--shadow-md); }
.service-detail-image { height: 250px; width: 100%; overflow: hidden; background: var(--blue-deep); }
.service-detail-image img { width: 100%; height: 100%; object-fit: cover; transition: transform 0.5s ease; }
.service-detail-card:hover .service-detail-image img { transform: scale(1.04); }
.service-detail-content { padding: 40px 30px 35px; position: relative; flex-grow: 1; display: flex; flex-direction: column; }
.service-detail-icon { width: 54px; height: 54px; background: rgba(217, 185, 109, 0.12); color: var(--gold-bright); border-radius: 14px; display: flex; align-items: center; justify-content: center; font-size: 1.45rem; margin-bottom: 20px; border: 1px solid rgba(217, 185, 109, 0.2); transition: all 0.3s ease; }
.service-detail-card:hover .service-detail-icon { background: var(--gold-main); color: var(--blue-dark); border-color: var(--gold-main); }
.service-detail-content h3 { font-size: 1.4rem; font-weight: 800; color: var(--blue-dark); margin-bottom: 14px; letter-spacing: -0.01em; }
body.dark-mode .service-detail-content h3 { color: var(--white); }
.service-detail-content p { color: var(--text-muted); font-size: 0.98rem; line-height: 1.6; margin-bottom: 24px; flex-grow: 1; }
.check-list { display: grid; gap: 14px; padding-top: 20px; border-top: 1px dashed var(--border-light); }
.check-list li { font-size: 0.92rem; color: var(--text-dark); display: flex; align-items: flex-start; gap: 12px; font-weight: 550; line-height: 1.45; }
.check-list li i { color: var(--gold-main); font-size: 0.9rem; margin-top: 3px; flex-shrink: 0; }

/* ========================================================= WHO WE SUPPORT & DAILY LIVING SECTIONS ========================================================= */
.daily-support-section { padding: clamp(60px, 8vh, 100px) 0; background: linear-gradient(135deg, var(--blue-dark), #0d233a); border-block: 1px solid var(--border-light); }
.daily-support-grid { display: grid; grid-template-columns: repeat(5, 1fr); gap: 24px; margin-top: 20px; }
.daily-support-grid:has(:nth-child(5):last-child) { display: flex; flex-wrap: wrap; justify-content: center; }
.daily-support-grid > * { flex: 1 1 calc(20% - 24px); min-width: 200px; max-width: calc(25% - 24px); }
.daily-support-card { background: rgba(255, 255, 255, 0.03); border: 1px solid rgba(255, 255, 255, 0.06); padding: 35px 24px; border-radius: var(--radius-xl); text-align: center; transition: all 0.3s ease; display: flex; flex-direction: column; align-items: center; }
.daily-support-card i { font-size: 2rem; color: var(--gold-bright); margin-bottom: 18px; }
.daily-support-card h3 { font-size: 1.15rem; font-weight: 700; color: var(--white); margin-bottom: 12px; letter-spacing: -0.01em; }
.daily-support-card p { color: rgba(255, 255, 255, 0.72); font-size: 0.88rem; line-height: 1.55; }
.daily-support-card:hover { background: rgba(255, 255, 255, 0.06); border-color: var(--gold-bright); transform: translateY(-6px); box-shadow: 0 10px 25px rgba(0,0,0,0.2); }

/* ========================================================= MICHIGAN PROGRAM & SERVICE GRIDS ========================================================= */
.program-section { padding: clamp(60px, 8vh, 100px) 0; background: var(--bg-main); }
.program-card { display: grid; grid-template-columns: 0.9fr 1.1fr; gap: 50px; background: var(--bg-card); border: 1px solid var(--border-light); border-radius: var(--radius-xl); overflow: hidden; box-shadow: var(--shadow-sm); align-items: center; }
.program-image { height: 100%; min-height: 440px; }
.program-image img { width: 100%; height: 100%; object-fit: cover; }
.program-content { padding: 50px; }
.program-content h3 { font-size: 1.55rem; font-weight: 800; color: var(--blue-dark); margin-bottom: 16px; letter-spacing: -0.02em; }
body.dark-mode .program-content h3 { color: var(--white); }
.program-content p { color: var(--text-muted); font-size: 1rem; line-height: 1.6; margin-bottom: 24px; }
.program-content .check-list { margin-bottom: 32px; }
.services-section { padding: clamp(60px, 8vh, 100px) 0; background: var(--bg-soft); }
.service-grid { display: grid; grid-template-columns: repeat(4, 1fr); gap: 24px; }
.service-card { background: var(--bg-card); border: 1px solid var(--border-light); border-radius: var(--radius-xl); box-shadow: var(--shadow-sm); transition: all 0.3s ease; }
.service-card:hover { transform: translateY(-5px); border-color: var(--border-gold); box-shadow: var(--shadow-md); }
.service-body { padding: 35px 30px; }
.service-body h3 { font-size: 1.25rem; font-weight: 700; color: var(--blue-dark); margin-bottom: 12px; }
body.dark-mode .service-body h3 { color: var(--white); }
.service-body p { color: var(--text-muted); font-size: 0.92rem; line-height: 1.55; }

/* ========================================================= CARE PROCESS SECTION ========================================================= */
.care-process-section { padding: clamp(60px, 8vh, 100px) 0; background: var(--bg-main); }
.process-grid { display: grid; grid-template-columns: repeat(4, 1fr); gap: 24px; position: relative; }
.process-card { background: var(--bg-card); border: 1px solid var(--border-light); padding: 40px 28px; border-radius: var(--radius-xl); position: relative; box-shadow: var(--shadow-sm); transition: border-color 0.3s; }
.process-card:hover { border-color: var(--border-gold); }
.process-card span { font-size: 2.4rem; font-weight: 900; color: rgba(217, 185, 109, 0.25); display: block; margin-bottom: 12px; font-family: monospace; line-height: 1; }
.process-card h3 { font-size: 1.2rem; font-weight: 700; color: var(--blue-dark); margin-bottom: 12px; }
body.dark-mode .process-card h3 { color: var(--white); }
.process-card p { color: var(--text-muted); font-size: 0.9rem; line-height: 1.55; }

/* ========================================================= CTA BANNER & COMPONENTS ========================================================= */
.service-cta-section { padding: 60px 0; background: var(--bg-soft); }
.service-cta-box { background: radial-gradient(circle at 10% 20%, rgba(217, 185, 109, 0.12), transparent 45%), linear-gradient(135deg, var(--blue-dark), var(--blue-deep)); border-radius: var(--radius-xl); border: 1px solid var(--border-gold); padding: 50px 60px; color: var(--white); display: flex; justify-content: space-between; align-items: center; gap: 40px; box-shadow: var(--shadow-lg); }
.service-cta-box h2 { font-size: clamp(1.6rem, 2.5vw, 2.2rem); font-weight: 800; letter-spacing: -0.02em; margin-bottom: 12px; color: var(--white); }
.service-cta-box p { color: rgba(255, 255, 255, 0.82); font-size: 1rem; line-height: 1.6; max-width: 660px; }

/* ========================================================= INTERACTIVE SUCCESS MODAL ========================================================= */
.modal-overlay { position: fixed; top: 0; left: 0; width: 100%; height: 100%; background: rgba(7, 24, 39, 0.6); backdrop-filter: blur(8px); display: flex; align-items: center; justify-content: center; z-index: 9999; opacity: 0; pointer-events: none; transition: opacity 0.3s ease; }
.modal-overlay.active { opacity: 1; pointer-events: auto; }
.success-modal { background: var(--bg-card); border: 1px solid var(--border-light); width: 100%; max-width: 440px; padding: 45px 35px; border-radius: var(--radius-xl); box-shadow: var(--shadow-lg); text-align: center; position: relative; transform: scale(0.9); transition: transform 0.3s ease; }
.modal-overlay.active .success-modal { transform: scale(1); }
.modal-close { position: absolute; top: 20px; right: 20px; background: none; border: none; font-size: 1.25rem; color: var(--text-muted); cursor: pointer; padding: 5px; line-height: 1; transition: color 0.2s; }
.modal-close:hover { color: var(--blue-dark); }
body.dark-mode .modal-close:hover { color: var(--white); }
.success-icon { width: 68px; height: 68px; background: rgba(217, 185, 109, 0.12); border: 2px solid var(--gold-bright); color: var(--gold-bright); border-radius: 50%; display: flex; align-items: center; justify-content: center; font-size: 1.65rem; margin: 0 auto 24px; }
.success-modal h2 { font-size: 1.55rem; font-weight: 800; color: var(--blue-dark); margin-bottom: 10px; letter-spacing: -0.02em; }
body.dark-mode .success-modal h2 { color: var(--white); }
.success-modal p { color: var(--text-muted); font-size: 1rem; line-height: 1.5; margin-bottom: 30px; }
.success-modal .btn { width: 100%; justify-content: center; }

/* ========================================================= GLOBAL FOOTER & SOCIAL LOGO ARCHITECTURE ========================================================= */
.footer { background: var(--blue-dark); color: var(--white); padding-top: 60px; position: relative; }
.newsletter-section { margin-bottom: 50px; }
.newsletter-box { background: rgba(255, 255, 255, 0.03); border: 1px solid rgba(255, 255, 255, 0.08); padding: 40px; border-radius: var(--radius-xl); display: flex; justify-content: space-between; align-items: center; gap: 30px; flex-wrap: wrap; }
.newsletter-box h2 { font-size: clamp(1.4rem, 2vw, 1.8rem); font-weight: 800; color: var(--white); letter-spacing: -0.02em; }
.newsletter-form { display: flex; gap: 12px; flex-grow: 1; max-width: 500px; width: 100%; }
.newsletter-form input { flex-grow: 1; padding: 14px 20px; background: rgba(7, 24, 39, 0.4); border: 1px solid rgba(255, 255, 255, 0.12); border-radius: var(--radius-md); color: var(--white); font-size: 0.95rem; outline: none; transition: border-color 0.25s; }
.newsletter-form input:focus { border-color: var(--gold-main); }
.newsletter-form button { padding: 14px 28px; background: linear-gradient(135deg, var(--gold-bright), var(--gold-main)); color: var(--blue-dark); border: none; border-radius: var(--radius-md); font-weight: 700; cursor: pointer; transition: transform 0.2s, box-shadow 0.2s; }
.newsletter-form button:hover { transform: translateY(-2px); box-shadow: 0 5px 15px rgba(217, 185, 109, 0.3); }

/* --- SEAMLESS STATIC TEXT-TO-ICON PSEUDO PIPELINE --- */
.social-footer { display: flex; justify-content: center; align-items: center; flex-wrap: wrap; gap: clamp(12px, 2vw, 20px); padding-bottom: 30px; border-bottom: 1px solid rgba(255, 255, 255, 0.08); } 
.social-footer a { color: transparent !important; text-shadow: none !important; font-size: 0px !important; background: rgba(255, 255, 255, 0.05); border: 1px solid rgba(225, 194, 124, 0.15); text-decoration: none; width: 44px; height: 44px; display: inline-flex; align-items: center; justify-content: center; border-radius: 50%; transition: all 0.28s ease; position: relative; overflow: hidden; } 
.social-footer a:hover { background: linear-gradient(135deg, var(--gold-bright), var(--gold-main)); border-color: var(--gold-bright); transform: translateY(-4px); box-shadow: 0 8px 20px rgba(194, 154, 77, 0.25); } 

/* Universal pseudo config loading fontawesome engine safely over direct text */
.social-footer a::before { font-family: "Font Awesome 5 Brands", "Font Awesome 6 Brands", "Font Awesome 5 Free", "Font Awesome 6 Free"; font-size: 1.2rem; color: var(--gold-bright); display: inline-block; transition: all 0.28s ease; line-height: 1; position: absolute; left: 50%; top: 50%; transform: translate(-50%, -50%); pointer-events: none; }
.social-footer a:hover::before { color: var(--blue-deep) !important; }

/* Hard mapped positional sequence forcing icons into exact order matching html tags */
.social-footer a:nth-of-type(1)::before { content: "\f39e"; } /* Facebook */
.social-footer a:nth-of-type(2)::before { content: "\f16d"; } /* Instagram */
.social-footer a:nth-of-type(3)::before { content: "\e61b"; } /* X / Twitter */
.social-footer a:nth-of-type(4)::before { content: "\e07b"; } /* TikTok */
.social-footer a:nth-of-type(5)::before { content: "\f167"; } /* YouTube */

.footer-main { display: grid; grid-template-columns: 1.2fr 0.8fr 1.2fr 0.8fr; gap: 40px; padding: 60px 0; }
.footer-brand img { width: 70px; height: 70px; object-fit: contain; margin-bottom: 18px; display: block; }
.footer-brand h3 { font-size: 1.25rem; font-weight: 800; color: var(--white); margin-bottom: 12px; }
.footer-brand p { color: rgba(255, 255, 255, 0.7); font-size: 0.92rem; line-height: 1.6; }
.footer-links { display: flex; flex-direction: column; gap: 12px; }
.footer-links h4 { font-size: 1.05rem; font-weight: 700; color: var(--gold-bright); text-transform: uppercase; letter-spacing: 0.08em; margin-bottom: 20px; }
.footer-links a { color: rgba(255, 255, 255, 0.75); text-decoration: none; font-size: 0.95rem; transition: color 0.2s, transform 0.2s; display: inline-flex; align-items: center; gap: 10px; width: fit-content; }
.footer-links a:hover { color: var(--gold-main); transform: translateX(4px); }
.contact-links a { display: flex; align-items: flex-start; width: 100%; gap: 12px; }
.contact-links i { color: var(--gold-main); font-size: 0.95rem; margin-top: 4px; flex-shrink: 0; display: inline-block; }
.contact-links span { line-height: 1.5; color: rgba(255, 255, 255, 0.75); }
.footer-bottom { background: rgba(0, 0, 0, 0.2); padding: 24px 0; text-align: center; border-top: 1px solid rgba(255, 255, 255, 0.05); }
.footer-bottom p { color: rgba(255, 255, 255, 0.5); font-size: 0.88rem; margin: 0; }

/* ========================================================= GLOBAL RESPONSIVE MEDIA QUERIES ========================================================= */
@media (max-width: 1200px) { .footer-main { grid-template-columns: repeat(2, 1fr); gap: 40px; } }
@media (max-width: 992px) { .hero-landscape-card { grid-template-columns: 1fr; min-height: auto; } .hero-landscape-card img { height: 300px; } .hero-overlay-content { padding: 40px 30px; } .services-detail-grid { grid-template-columns: 1fr; gap: 30px; } .daily-support-grid > * { flex: 1 1 calc(33.33% - 24px); max-width: calc(33.33% - 24px); } .program-card { grid-template-columns: 1fr; gap: 0; } .program-image { min-height: 300px; height: 300px; } .program-content { padding: 40px 30px; } .service-grid, .process-grid { grid-template-columns: repeat(2, 1fr); gap: 24px; } .service-cta-box { flex-direction: column; text-align: center; justify-content: center; padding: 45px 30px; gap: 28px; } .newsletter-box { flex-direction: column; text-align: center; align-items: center; padding: 35px 25px; } .newsletter-form { max-width: 100%; } }
@media (max-width: 768px) { .daily-support-grid > * { flex: 1 1 calc(50% - 24px); max-width: calc(50% - 24px); } .footer-main { grid-template-columns: 1fr; gap: 35px; } .footer-links h4 { margin-bottom: 14px; } }
@media (max-width: 550px) { .service-grid, .process-grid, .daily-support-grid > * { flex: 1 1 100%; max-width: 100%; grid-template-columns: 1fr; } .hero-buttons { flex-direction: column; } .hero-buttons .btn { width: 100%; justify-content: center; } }

/* ========================================================= GLOBAL RESPONSIVE MEDIA QUERIES ========================================================= */
@media (max-width: 1200px) { .footer-main { grid-template-columns: repeat(2, 1fr); gap: 40px; } }
@media (max-width: 992px) { .hero-landscape-card { grid-template-columns: 1fr; min-height: auto; } .hero-landscape-card img { height: 300px; } .hero-overlay-content { padding: 40px 30px; } .services-detail-grid { grid-template-columns: 1fr; gap: 30px; } .daily-support-grid > * { flex: 1 1 calc(33.33% - 24px); max-width: calc(33.33% - 24px); } .program-card { grid-template-columns: 1fr; gap: 0; } .program-image { min-height: 300px; height: 300px; } .program-content { padding: 40px 30px; } .service-grid, .process-grid { grid-template-columns: repeat(2, 1fr); gap: 24px; } .service-cta-box { flex-direction: column; text-align: center; justify-content: center; padding: 45px 30px; gap: 28px; } .newsletter-box { flex-direction: column; text-align: center; align-items: center; padding: 35px 25px; } .newsletter-form { max-width: 100%; } }
@media (max-width: 768px) { .daily-support-grid > * { flex: 1 1 calc(50% - 24px); max-width: calc(50% - 24px); } .footer-main { grid-template-columns: 1fr; gap: 35px; } .footer-links h4 { margin-bottom: 14px; } }
@media (max-width: 550px) { .service-grid, .process-grid, .daily-support-grid > * { flex: 1 1 100%; max-width: 100%; grid-template-columns: 1fr; } .hero-buttons { flex-direction: column; } .hero-buttons .btn { width: 100%; justify-content: center; } }


/* ==========================================================================
 Contact.html
========================================================================== */

/* ========================================================= CONTACT PAGE ARCHITECTURE & HERO ========================================================= */
.contact-page-hero { padding: clamp(40px, 6vh, 80px) 0; background: var(--bg-soft); }
.contact-page-form-section { padding: clamp(60px, 8vh, 100px) 0; background: var(--bg-main); }

/* ========================================================= TWO-COLUMN SPLIT CONTACT LAYOUT ========================================================= */
.contact-layout { display: grid; grid-template-columns: 1fr 1.3fr; gap: 40px; align-items: start; margin-top: 20px; }
.contact-info-card { background: var(--bg-card); border: 1px solid var(--border-light); padding: 40px; border-radius: var(--radius-xl); box-shadow: var(--shadow-sm); }
.contact-info-card h3 { font-size: 1.5rem; font-weight: 800; color: var(--blue-dark); margin-bottom: 14px; letter-spacing: -0.02em; }
body.dark-mode .contact-info-card h3 { color: var(--white); }
.contact-info-card > p { color: var(--text-muted); font-size: 0.98rem; line-height: 1.6; margin-bottom: 30px; }
.contact-info-list { display: flex; flex-direction: column; gap: 20px; margin-bottom: 35px; border-bottom: 1px dashed var(--border-light); padding-bottom: 30px; }
.contact-info-list p { font-size: 0.98rem; color: var(--text-dark); display: flex; align-items: flex-start; gap: 14px; line-height: 1.5; }
.contact-info-list i { color: var(--gold-main); font-size: 1.1rem; margin-top: 3px; flex-shrink: 0; width: 20px; text-align: center; }
.contact-info-list a { color: var(--text-dark); text-decoration: none; transition: color 0.2s; }
.contact-info-list a:hover { color: var(--gold-main); }
.contact-mini-card { display: flex; gap: 16px; align-items: flex-start; padding: 20px; background: var(--bg-soft); border-radius: var(--radius-lg); margin-bottom: 16px; border: 1px solid var(--border-light); }
.contact-mini-card:last-child { margin-bottom: 0; }
.contact-mini-card i { font-size: 1.4rem; color: var(--gold-bright); flex-shrink: 0; margin-top: 2px; }
.contact-mini-card h4 { font-size: 1.05rem; font-weight: 700; color: var(--blue-dark); margin-bottom: 6px; }
body.dark-mode .contact-mini-card h4 { color: var(--white); }
.contact-mini-card p { color: var(--text-muted); font-size: 0.9rem; line-height: 1.5; }

/* ========================================================= DYNAMIC CONTACT INPUT FORM & WRAPPERS ========================================================= */
.form-card { background: var(--bg-card); border: 1px solid var(--border-light); padding: 45px 40px; border-radius: var(--radius-xl); box-shadow: var(--shadow-sm); }
.care-form { display: flex; flex-direction: column; gap: 24px; }
.form-row { display: grid; grid-template-columns: repeat(2, 1fr); gap: 20px; }
.form-group { display: flex; flex-direction: column; gap: 8px; position: relative; }
.form-group label { font-size: 0.92rem; font-weight: 650; color: var(--text-dark); }
.form-group input, .form-group select, .form-group textarea { width: 100%; padding: 12px 16px; background: var(--bg-main); border: 1px solid var(--border-light); border-radius: var(--radius-md); color: var(--text-main); font-size: 0.95rem; font-family: inherit; transition: all 0.25s ease; outline: none; }
.form-group select { appearance: none; background-image: url("data:image/svg+xml;charset=UTF-8,%3csvg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 24 24' fill='none' stroke='%23bf9b53' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'%3e%3cpolyline points='6 9 12 15 18 9'%3e%3c/polyline%3e%3c/svg%3e"); background-repeat: no-repeat; background-position: right 16px center; background-size: 16px; padding-right: 45px; cursor: pointer; }
.form-group input:focus, .form-group select:focus, .form-group textarea:focus { border-color: var(--gold-main); box-shadow: 0 0 0 3px rgba(217, 185, 109, 0.12); background: var(--bg-card); }
.form-group textarea { resize: vertical; min-height: 120px; }
.field-error { font-size: 0.8rem; color: #dc3545; font-weight: 550; display: block; margin-top: 4px; min-height: 16px; }

/* ========================================================= SMS CONSENT OPT-IN WRAPPER ========================================================= */
.sms-checkbox-group { display: flex; align-items: flex-start; gap: 12px; margin-top: 6px; }
.sms-checkbox-group input[type="checkbox"] { width: 18px; height: 18px; border: 1px solid var(--border-light); border-radius: 4px; background: var(--bg-main); cursor: pointer; accent-color: var(--gold-main); flex-shrink: 0; margin-top: 2px; }
.sms-checkbox-group label { font-size: 0.85rem; line-height: 1.5; color: var(--text-muted); cursor: pointer; user-select: none; }
.checkbox-error { margin-top: -12px; margin-bottom: 4px; }
.submit-btn { width: 100%; justify-content: center; padding: 14px; font-size: 1rem; }

/* ========================================================= SUPPORT AREAS GRID ========================================================= */
.contact-service-area-section { padding: clamp(60px, 8vh, 100px) 0; background: var(--bg-soft); }
.area-grid { display: grid; grid-template-columns: repeat(4, 1fr); gap: 24px; }
.area-card { background: var(--bg-card); border: 1px solid var(--border-light); padding: 35px 24px; border-radius: var(--radius-xl); text-align: center; box-shadow: var(--shadow-sm); transition: all 0.3s ease; display: flex; flex-direction: column; align-items: center; justify-content: center; }
.area-card i { font-size: 2.2rem; color: var(--gold-bright); margin-bottom: 16px; transition: transform 0.3s ease; }
.area-card h3 { font-size: 1.15rem; font-weight: 700; color: var(--blue-dark); letter-spacing: -0.01em; }
body.dark-mode .area-card h3 { color: var(--white); }
.area-card:hover { transform: translateY(-5px); border-color: var(--border-gold); box-shadow: var(--shadow-md); }
.area-card:hover i { transform: scale(1.08); }

/* ========================================================= CONTACT PAGE CTA MODIFICATION ========================================================= */
.contact-cta-section { padding: 60px 0; background: var(--bg-soft); }
.contact-cta-box { background: radial-gradient(circle at 10% 20%, rgba(217, 185, 109, 0.12), transparent 45%), linear-gradient(135deg, var(--blue-dark), var(--blue-deep)); border-radius: var(--radius-xl); border: 1px solid var(--border-gold); padding: 50px 60px; color: var(--white); display: flex; justify-content: space-between; align-items: center; gap: 40px; box-shadow: var(--shadow-lg); }
.contact-cta-box h2 { font-size: clamp(1.6rem, 2.5vw, 2.2rem); font-weight: 800; letter-spacing: -0.02em; margin-bottom: 12px; color: var(--white); }
.contact-cta-box p { color: rgba(255, 255, 255, 0.82); font-size: 1rem; line-height: 1.6; max-width: 660px; }

/* ========================================================= LAYOUT RESPONSIVE STRATEGIES ========================================================= */
@media (max-width: 992px) { .contact-layout { grid-template-columns: 1fr; gap: 30px; } .form-card { padding: 35px 25px; } .area-grid { grid-template-columns: repeat(2, 1fr); gap: 20px; } .contact-cta-box { flex-direction: column; text-align: center; justify-content: center; padding: 45px 30px; gap: 28px; } }
@media (max-width: 600px) { .form-row { grid-template-columns: 1fr; gap: 24px; } .area-grid { grid-template-columns: 1fr; gap: 16px; } .contact-info-card { padding: 30px 20px; } .contact-mini-card { padding: 16px; } }